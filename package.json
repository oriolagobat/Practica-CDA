{
  "name": "university-practices",
  "private": true,
  "version": "0.71.0",
  "scripts": {
    "build": "rm -rf ./dist && tsc --build tsconfig.json",
    "clean": "rm -rf ./dist && rm -rf node_modules",
    "lint": "eslint 'src/**/*.ts' --quiet --fix"
  },
  "dependencies": {
    "aws-sdk": "^2.1091.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.93",
    "@types/aws-sdk": "^2.7.0",
    "@types/node": "^17.0.21"
  }
}
